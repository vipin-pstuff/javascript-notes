
// practice time
/*  
    ques : if the argument is a positive number ,
        Math.trunc() is equivalent to Math.floor()
        otherwise Math.trunc() is equivalent to Math.ceil()
*/
// solution
/* 
    - if we have positive decimal number then
        trunc() method will work like Math.floor()
    - but if we have negative decimal number then
        trunc() method will work like Math.ceil() 

    - so in Math.trunc() and Math.ceil() method give number before decimal point
        and little bit same thing happen in Math.floor()
*/

